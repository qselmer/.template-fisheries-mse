# Operating Model specification

> **Primary documentation language:** Spanish · [Versión en español](README.md)

This folder states the minimum scientific processes that the simulated true system must be able to represent. `specification.yml` is a checklist, not an executable model. The scientific rationale belongs in `docs/08_operating_model/`, while concrete run settings belong in `config/operating_model.yml`.
