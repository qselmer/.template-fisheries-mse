# Observation-model specification

> **Primary documentation language:** Spanish · [Versión en español](README.md)

This folder states what the observation model must be able to represent when converting simulated true states into imperfect data. `specification.yml` is a requirements checklist, while numerical settings belong in `config/observation_model.yml` and scientific justification belongs in `docs/09_observation_model/`.
